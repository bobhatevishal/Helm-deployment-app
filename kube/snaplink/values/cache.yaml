nameOverride: "redis"
namespace: "snaplink-ns"

global:
  imageRegistry: <registry>

image:
  repository: <repo_name>
  tag: <image_tag>

service:
  port: 6379

probeType: "tcp"
ingress:
  enabled: false